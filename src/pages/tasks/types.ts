export enum TaskStatus {
    active = "ativo",
    inactive = "inativo",
    completed = "completo",
    pending = "pendente",
    blocked = "bloqueado"
}

export type TaskStatusTypes = 'active' | 'inactive' | 'completed' | 'pending' | 'blocked';